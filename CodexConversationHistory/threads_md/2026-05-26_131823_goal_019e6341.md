# GOAL

## Metadata

- Thread ID: `019e6341-9854-76c2-ac17-499b548895a3`
- Created: 2026-05-26 13:18:23 IST
- Updated: 2026-05-26 14:19:27 IST
- CWD: `/Users/satvik.bansal/Desktop/Handy.android/HandyV2`
- Source rollout: `/Users/satvik.bansal/.codex/sessions/2026/05/26/rollout-2026-05-26T13-18-23-019e6341-9854-76c2-ac17-499b548895a3.jsonl`
- Recorded git branch at start: `main`
- Recorded git SHA at start: `f5b206b4f26ca1c66ca906dde7a0059bfcc59731`
- Messages exported: 44

## Brief Summary

This conversation focused on: Make Handy's transcription explicit, testable, and Hindi/Hinglish-friendly. Likely related git changes: 0101729 P-STT1: STT mode, language, MAX_RESULTS=3, confidence, Andr….

## Git Commit Linkage

Commit links are heuristic: Codex records the starting git SHA, while likely related commits are inferred from timing plus title/commit-subject matching. Review the commit list before treating it as authoritative.

- Base SHA recorded by Codex: `f5b206b4f26ca1c66ca906dde7a0059bfcc59731`
- Likely related commits:
  - `0101729` 2026-05-26 14:21:30 IST [medium] P-STT1: STT mode, language, MAX_RESULTS=3, confidence, Andr…. Files: DEBUG_LOG.md, android-runtime/build.gradle.kts, android-runtime/src/main/kotlin/com/handy/runtime/di/RuntimeModule.kt, android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidSttClient.kt, android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidTtsClient.kt, android-runtime/src/main/kotlin/com/handy/runtime/storage/DataStoreSettings.kt, android-runtime/src/test/kotlin/com/handy/runtime/speech/AndroidSttClientTest.kt, android-runtime/src/test/kotlin/com/handy/runtime/speech/TtsChunkerTest.kt, app/src/main/AndroidManifest.xml, app/src/main/kotlin/com/handy/app/chat/ChatViewModel.kt, app/src/main/kotlin/com/handy/app/diagnostics/DiagnosticsActivity.kt, app/src/main/kotlin/com/handy/app/overlay/FloatingWidgetOverlayService.kt

## Conversation

### USER 2026-05-26T07:48:26.703Z

GOAL
Make Handy's transcription explicit, testable, and Hindi/Hinglish-friendly.
Do NOT add cloud STT providers in this prompt (that's P-STT-2). All
changes are within the existing AndroidSttClient and its plumbing.

VERIFIED PRE-CONDITIONS
- android-runtime/.../speech/AndroidSttClient.kt
    languageTag default = "en-US" (line 38)
    EXTRA_MAX_RESULTS = 1 (line 90)
    DL-015 (lines 112-134) — on-device language-pack-missing → cloud
      fallback in-place.
    DL-024 (lines 145-164) — second-tier retry with EXTRA_PREFER_OFFLINE=false.
- core/src/main/kotlin/com/handy/core/speech/SttClient.kt
    interface SttClient. SttEvent.Final(transcript: String). No alternatives,
    no confidence.
- android-runtime/.../RuntimeModule.kt:189-190
    provideSttClient takes only Context. No settings.

IMPLEMENTATION

1. Add settings:
   File: core/src/main/kotlin/com/handy/core/model/Settings.kt
       enum class SttMode(val displayName: String) {
           AUTO("Auto (on-device first)"),
           ON_DEVICE_ONLY("On-device only (private)"),
           NETWORK_ALLOWED("Network allowed"),
       }
       enum class SttLanguage(val tag: String) {
           SYSTEM(Locale.getDefault().toLanguageTag()),
           ENGLISH("en-US"),
           HINDI("hi-IN"),
           HINGLISH("hi-IN"),   // hi-IN but with language-switch extras on Android 14+
       }
       val sttMode: SttMode = SttMode.AUTO,
       val sttLanguage: SttLanguage = SttLanguage.SYSTEM,
   Migrate the existing SttProvider (currently ANDROID only) — leave that
   enum intact; sttMode is orthogonal (provider = Android, mode = how it runs).

2. Extend SttEvent with alternatives/confidence:
   File: core/src/main/kotlin/com/handy/core/speech/SttClient.kt
       data class Final(
           val transcript: String,
           val alternatives: List<String> = emptyList(),
           val confidence: Float? = null,
           val isOnDevice: Boolean = false,
       ) : SttEvent()
   Existing call sites (VoiceController) read `transcript` — keep the
   default values so binary compatibility holds.

3. Rewrite AndroidSttClient.listen() to accept mode + language:
   File: android-runtime/.../speech/AndroidSttClient.kt

   Constructor:
       class AndroidSttClient(
           private val context: Context,
           private val settingsProvider: () -> AndroidSttConfig,
       )
       data class AndroidSttConfig(
           val mode: SttMode,
           val languageTag: String,
           val enableLanguageSwitch: Boolean,
       )

   listen() resolves config at session start (so per-session mode changes
   take effect on the NEXT listen, not mid-session — keeps the recognizer
   contract simple).

   Mode behaviour:
   - AUTO: existing two-tier fallback (DL-015 + DL-024) stays. Document.
   - ON_DEVICE_ONLY: if `isOnDeviceRecognitionAvailable(context)` is false
     OR onDeviceDisabled flag set OR API < 31 → emit SttEvent.Error(
       "On-device speech isn't available on this phone for this language.",
       isRecoverable = false) and close the flow. Do NOT fall back.
     If on-device returns ERROR_LANGUAGE_UNAVAILABLE → emit
       SttEvent.Error("On-device speech is missing the language pack.",
       false). Do NOT fall back.
   - NETWORK_ALLOWED: skip on-device entirely, go straight to cloud
     recognizer. (Useful for users who explicitly want better network
     accuracy.)

   Intent extras:
   - EXTRA_LANGUAGE = config.languageTag
   - EXTRA_MAX_RESULTS = 3
   - On API 31+: EXTRA_PREFER_OFFLINE only when mode=AUTO and we picked
     on-device (existing logic, no change).
   - On API 33+ (TIRAMISU+): putExtra("android.speech.extra.ENABLE_FORMATTING",
     "quality") if available. Guard by SDK_INT.
   - On API 34+ (UPSIDE_DOWN_CAKE+): if config.enableLanguageSwitch,
     putExtra("android.speech.extra.ENABLE_LANGUAGE_SWITCH", true) and
     putExtra("android.speech.extra.LANGUAGE_SWITCH_ALLOWED_LANGUAGES",
     arrayListOf("en-IN", "hi-IN")). Guard by SDK_INT and recognizer
     support (catch SecurityException, log, continue without the extra).

   Result parsing:
   - SpeechRecognizer.RESULTS_RECOGNITION → top 3 alternatives.
   - SpeechRecognizer.CONFIDENCE_SCORES → FloatArray; pair with
     alternatives; take the first's confidence for SttEvent.Final.confidence.
   - emit SttEvent.Final(transcript=best, alternatives=rest, confidence=…,
     isOnDevice=usedOnDevice).

4. DI rewire:
   File: android-runtime/.../RuntimeModule.kt
       @Provides @Singleton
       fun provideSttClient(
           @ApplicationContext context: Context,
           settings: DataStoreSettings,
       ): SttClient {
           val provider = {
               val s = settings.current()
               AndroidSttConfig(
                   mode = s.sttMode,
                   languageTag = if (s.sttLanguage == SttLanguage.SYSTEM)
                       Locale.getDefault().toLanguageTag() else s.sttLanguage.tag,
                   enableLanguageSwitch = (s.sttLanguage == SttLanguage.HINGLISH),
               )
           }
           return AndroidSttClient(context, provider)
       }

5. Low-confidence confirmation chip:
   File: app/src/main/kotlin/com/handy/app/voice/VoiceController.kt
   - Add a threshold constant: LOW_CONFIDENCE = 0.55f.
   - When SttEvent.Final has confidence != null && confidence < LOW_CONFIDENCE
     AND alternatives.isNotEmpty():
       - Do not auto-submit.
       - Set a new presenter state:
           presenter.onLowConfidenceTranscript(best, alternatives)
       - In OverlayPresenter, render a chip in the panel: "I heard '<best>'.
         Did you mean: <alt1>? <alt2>?". User picks one or types over.
   - When confidence is null (older recognizers), preserve current behavior.

6. Settings UI:
   File: app/src/main/kotlin/com/handy/app/settings/sections/BrainSection.kt
   Add under the existing STT row:
   - STT mode picker: Auto / On-device only / Network allowed.
   - Language picker: System / English / Hindi / Hinglish.
   - Subtitle: "Hinglish enables Android's code-mix recognition where
     supported (Android 14+)."

ACCEPTANCE
- ON_DEVICE_ONLY with no English pack on the device → user sees error,
  no network call made (network inspector confirms).
- AUTO with language pack present → uses on-device.
- AUTO with language pack missing → falls back to cloud (existing
  behaviour preserved).
- NETWORK_ALLOWED → skips on-device.
- Hindi-only setting → speech recognizer uses hi-IN.
- Hinglish setting → on API 34+ enables language switch; below 34 falls
  back to hi-IN single-language recognition.
- MAX_RESULTS=3 → alternatives populated in SttEvent.Final.
- Low confidence with alternatives → confirmation chip shown, no LLM
  call until user picks.
- High confidence → submitted automatically (current behaviour).

TESTS
- AndroidSttClientTest (Robolectric, with a fake RecognitionListener-like
  shim):
  - mode=ON_DEVICE_ONLY + isOnDeviceRecognitionAvailable=false → Error,
    no cloud recognizer constructed.
  - mode=ON_DEVICE_ONLY + onDevice returns ERROR_LANGUAGE_UNAVAILABLE
    → Error, NO retry path.
  - mode=AUTO + onDevice returns ERROR_LANGUAGE_UNAVAILABLE → existing
    fallback path (DL-015) still works.
  - languageTag threaded into Intent EXTRA_LANGUAGE.
  - SDK_INT < 34 → language-switch extras absent.
  - SDK_INT >= 34 + Hinglish → language-switch extras present.
  - bestResult parses top 3 + confidence array correctly.
- VoiceControllerTest:
  - low-confidence Final with alternatives → presenter.onLowConfidenceTranscript
    called, no submission to chat.
  - high-confidence Final → submitted directly.
- Manual:
  - Settings: Auto + English → say "set a five minute timer" → submits.
  - Settings: On-device only on a fresh emulator with no English pack
    → error chip, no submission.
  - Settings: Hindi → say "panch minute ka timer lagao" → recognised
    in hi-IN.
  - Settings: Hinglish on Pixel running Android 14 → say
    "Subah saat baje alarm set kar do" → recognised.

DO NOT
- Add a cloud STT provider (P-STT-2 handles that).
- Persist audio.
- Weaken DL-015 / DL-024 in AUTO mode — those tiers are important on
  emulators.
- Break the existing long-press / partial / final flow.

UPDATE
- DEBUG_LOG.md
- Settings strings. 

Additional stuff
A) Mention the change the code brings to the application - the before and after with real life practical examples if possible. 
B) Share an indepth end to end manual testing plan of the changes done - be practical and ensure all aspects are tested well before we proceed.   
C) ENSURE THERE ARE NO BUILDS ERRORS OR APP CRASHES. AUDIOT THE CODE COMPLETELY AND FIX ANY ERRORS

### ASSISTANT 2026-05-26T07:48:33.085Z

I’ll trace the existing STT, settings, and overlay flow first so the change lands in the shape of this app, then I’ll patch and run the relevant tests/build checks.

### ASSISTANT 2026-05-26T07:48:48.630Z

I found the target files and the worktree only has an untracked `handy_macos_ref/`, so I’ll leave that alone. Now I’m reading the current STT lifecycle, settings persistence, and presenter API before editing.

### ASSISTANT 2026-05-26T07:49:20.937Z

The current voice path stores only a string and the panel bridge auto-submits whatever `stopAndAwaitFinal()` returns. The cleanest low-confidence hook is to let `VoiceController` return “needs confirmation” as no transcript while pushing a presenter state into the panel.

### ASSISTANT 2026-05-26T07:54:27.253Z

I’m going to make the STT adapter testable by adding a tiny recognizer factory seam, then wire settings through DataStore and the runtime provider. That keeps the production path on Android’s `SpeechRecognizer` while letting unit tests drive listener callbacks deterministically.

### ASSISTANT 2026-05-26T07:57:28.744Z

The core STT model and runtime adapter are now shaped around explicit mode/language config. Next I’m adding the low-confidence transcript state so the recognizer can stop the auto-submit path and ask the user to choose instead.

### ASSISTANT 2026-05-26T08:04:20.814Z

The low-confidence path now holds back the transcript and keeps the chip alive instead of letting the overlay bridge clear it as a “no transcript” case. I’ve also added the settings rows and recorded the change in `DEBUG_LOG.md`; now I’m moving into compile/test cleanup.

### ASSISTANT 2026-05-26T08:04:41.607Z

`git diff --check` is clean. I’m starting with the targeted STT and voice-controller tests, because they’ll catch both Kotlin compile mistakes and the behavior this change is meant to lock.

### ASSISTANT 2026-05-26T08:06:58.309Z

Targeted STT and voice-controller tests pass, and the new code compiled cleanly in both touched modules. I’m running the broader unit/lint/debug build now so we catch integration errors outside the exact test names too.

### ASSISTANT 2026-05-26T08:08:11.140Z

The full `:core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` run is green. I’m checking whether there’s a device/emulator attached so I can do a launch smoke for crashes too.

### ASSISTANT 2026-05-26T08:08:36.665Z

There is an emulator attached; I’ll install the freshly built debug APK and do a quick launch/logcat smoke so the “no app crash” claim is backed by a real process start, not just compilation.

### ASSISTANT 2026-05-26T08:11:04.079Z

I made one tiny presenter hardening tweak after the full run so the chip can never be hidden behind a stale error banner. The affected app code recompiled and its voice tests passed; I’m re-running lint/assemble for the final post-tweak check.

### ASSISTANT 2026-05-26T08:11:38.095Z

Post-tweak lint and debug assemble are green. I’m reinstalling the final APK once more and repeating the launch smoke so the runtime check matches the exact build on disk.

### ASSISTANT 2026-05-26T08:13:25.574Z

Implemented the STT upgrade end to end.

**What Changed**
Before: Handy always used Android STT as implicit `en-US`, asked for only one result, and auto-submitted the final transcript even if Android had low confidence.

After: Handy now has explicit STT settings and richer final results:
- STT mode: Auto, On-device only, Network allowed.
- STT language: System, English, Hindi, Hinglish.
- Android STT now requests top 3 results, parses alternatives/confidence, and marks whether the result came from on-device recognition.
- AUTO keeps DL-015 and DL-024 fallback behavior.
- ON_DEVICE_ONLY fails closed with no network fallback.
- NETWORK_ALLOWED skips on-device.
- Hinglish uses `hi-IN`, and on Android 14+ enables language switching for `en-IN`/`hi-IN`.
- Low-confidence finals with alternatives now show a confirmation chip instead of going to the LLM.

Example: before, “panch minute ka timer lagao” still went through `en-US` and often became nonsense. Now Hindi mode sends `hi-IN`. Hinglish on Android 14+ can handle “Subah saat baje alarm set kar do” with code-mix extras enabled.

Key files touched include [AndroidSttClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidSttClient.kt), [SttClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/core/src/main/kotlin/com/handy/core/speech/SttClient.kt), [Settings.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/core/src/main/kotlin/com/handy/core/model/Settings.kt), [VoiceController.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/voice/VoiceController.kt), and [BrainSection.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/settings/sections/BrainSection.kt).

**Verification**
Passed:
- `git diff --check`
- Targeted `AndroidSttClientTest` and `VoiceControllerTest`
- Full `:core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`
- Final post-tweak `:app:lintDebug :app:assembleDebug`
- Installed final debug APK on `emulator-5554`, launched onboarding, checked logcat: no fatal exception, no AndroidRuntime crash, no ANR, no process death; Handy process stayed live.

I left the pre-existing untracked `handy_macos_ref/` untouched.

**Manual Test Plan**
1. Settings smoke: open Settings, verify AI Brain shows STT mode and Recognition language pickers. Toggle all modes/languages, leave Settings, reopen, confirm persistence.

2. Auto + English: set Auto + English. Say “set a five minute timer.” Expect normal auto-submit and no confirmation chip on clear speech.

3. Auto + pack present: on a device with English on-device pack installed, speak offline. Expect on-device path and successful final.

4. Auto + pack missing: use a fresh emulator with no speech pack. Say the same phrase. Expect fallback to system/cloud recognizer, preserving existing emulator behavior.

5. On-device only + no pack: set On-device only + English on a fresh emulator. Speak. Expect error: “On-device speech is missing the language pack” or unavailable-device error. Confirm no transcript submission.

6. Network isolation check: repeat On-device only with network enabled and Android Studio Network Inspector open. Also repeat in airplane mode. Expected behavior should be the same fail-closed error, not a slower cloud attempt.

7. Network allowed: set Network allowed + English. Speak. Confirm it skips on-device. On fresh emulator, it should not hit the on-device pack-missing fallback path first.

8. Hindi: set Hindi. Say “panch minute ka timer lagao.” Confirm recognizer uses `hi-IN` and transcript is Hindi/Hinglish-friendly enough to submit.

9. Hinglish API 34+: on Pixel/Android 14+, set Hinglish. Say “Subah saat baje alarm set kar do.” Confirm recognition works and log/test instrumentation shows language-switch extras present.

10. Hinglish below API 34: on API 33 or lower, set Hinglish. Confirm no language-switch extras, but `hi-IN` single-language recognition still runs.

11. Low confidence: induce noisy speech so Android returns confidence below `0.55` with alternatives. Expect chip: “I heard ‘...’. Did you mean: ...” and no LLM call until a chip or typed override is submitted.

12. Regression flow: long-press widget, partial transcript, release, final submit; cancel voice; panel mic start/stop; full chat voice start/stop. Confirm no swallowed final transcript and no stuck listening state.

### USER 2026-05-26T08:13:25.782Z

Lots of changes done above which included context compactions in the middle. 1300+ lines of code were written across 23+ files- that's a lot of code and it needs a deep deep thorough audit - functional as well as non-functional! The voice services especially are critical in Handy! Can you go through the original prompt, code changes done in extreme detail, audit them, ensure all the required changes were done and done correctly with no downstream impact. the code before this works fine with it - no suspicious lines of codes for app crashes, or faulty wirings. strengthen the STT really well so it works accurately. all the services that needed to be changed have been changed and nothing is left or missed. ensure the ui is also proper with correct spacings, colours, fonts and elements. be very thorough in your analysis and evaluation. use highest level of reasoning available. Log the rca in the debug log and explain why the misses were missed. use highest level of thinking possible and think deeply. Evaluate and understand very deeply. Be very thorough and detailed. use your highest level of intelligence to analyse the requirement, code generated and plug gaps. be very thorough and have highest attention to detail. carefully analyse all the code - new and old and all dependencies too.

### ASSISTANT 2026-05-26T08:13:46.404Z

I’ll do this as an audit-first pass: re-read the requirement against every touched path, identify gaps, patch anything real, then re-run tests/build/smoke and append a new debug-log RCA.

### ASSISTANT 2026-05-26T08:15:14.751Z

I found a couple of practical polish gaps in the confirmation flow: the “type over” field wasn’t prefilled with the heard text, and choosing a chip could leave that draft cached in presenter state. I’m patching that and tightening the bridge so a failed submit doesn’t clear the chip prematurely.

### ASSISTANT 2026-05-26T08:18:33.856Z

I’m picking up from the deep audit pass now. I’ll verify the actual tree first, then rerun targeted and full validation so the final result is based on the code as it exists on disk, not memory from the earlier compaction.

### ASSISTANT 2026-05-26T08:19:01.581Z

The diff is the expected STT-heavy surface, plus the unrelated untracked `handy_macos_ref/` still sitting outside this work. I’m going line-by-line through the runtime client, DI, presenter bridge, and tests now because those are the places most likely to create silent regressions.

### ASSISTANT 2026-05-26T08:21:55.475Z

A few concrete audit gaps surfaced: the new `Final` shape should expose a real one-argument JVM constructor for old compiled call sites, DL-024 deserved its own regression test, language-switch SecurityException fallback wasn’t covered, and the “older recognizer gives no confidence” path needed a direct VoiceController test. I’m patching those before the full verification run.

### ASSISTANT 2026-05-26T08:22:53.850Z

The audit hardening patches are in: the result model now has a real overload for old one-argument construction, Android STT tests cover both DL-015 and DL-024 plus Android 14 language-switch fallback, and VoiceController now explicitly proves null confidence still auto-submits. Next I’m doing mechanical checks, then the targeted test set.

### ASSISTANT 2026-05-26T08:24:24.127Z

Targeted STT and presenter/voice tests are green after the extra coverage. I’m doing one more static sweep for crash-prone patterns in the touched files, then I’ll run the full repo validation and device smoke.

### ASSISTANT 2026-05-26T08:24:57.488Z

The full run found one app unit-test failure in `ChatViewModelVoiceTest` around `Dispatchers.Main`, so I’m not treating the build as clean yet. I’m going into that test and the voice stop path now; this is exactly the kind of downstream edge the audit was meant to catch.

### ASSISTANT 2026-05-26T08:26:09.016Z

The failure is a test cleanup race, not an STT crash: `ChatViewModel` has real `Dispatchers.IO` hops during `send`, and the test reset `Dispatchers.Main` before all `viewModelScope` children had joined. I’m tightening that test cleanup so the suite reflects app behavior instead of racing the test dispatcher.

### ASSISTANT 2026-05-26T08:26:45.895Z

That cleanup race is fixed and the targeted ChatViewModel voice test now passes. I’m rerunning the full `core + android-runtime + app + lint + assemble` command from scratch so the validation ledger is clean.

### ASSISTANT 2026-05-26T08:27:26.720Z

Full validation is now green, including lint and `assembleDebug`. I’m installing the rebuilt APK on the emulator and doing a launch/logcat smoke next, because voice plumbing bugs that compile can still show up as app-start wiring crashes.

### ASSISTANT 2026-05-26T08:27:47.785Z

The rebuilt app installs and the onboarding launch stays alive with a clean fatal/ANR scan. I’m also opening Settings directly and grabbing a screenshot pass of the STT rows so the UI part of the audit is grounded in a real rendered screen, not just Compose code.

### ASSISTANT 2026-05-26T08:30:13.053Z

Deep audit done. I found and fixed several real gaps, then reran validation end to end.

Key hardening:
- [AndroidSttClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidSttClient.kt:59): preserved AUTO DL-015/DL-024 behavior, added explicit DL-024 test coverage, added Android 14 language-switch fallback coverage.
- [RuntimeModule.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/di/RuntimeModule.kt:197): changed STT config to read `DataStoreSettings.current()` per listen session, avoiding stale first-session defaults.
- [SttClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/core/src/main/kotlin/com/handy/core/speech/SttClient.kt:48): added `@JvmOverloads` so old one-argument `Final(transcript)` construction remains ABI-safe.
- [OverlayPresenter.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/overlay/OverlayPresenter.kt:263): low-confidence chip now pre-fills the draft so users can actually type over the heard phrase.
- [OverlayPanelBridge.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/overlay/OverlayPanelBridge.kt:57): confirmation state clears only after a submission emit succeeds.
- [ChatViewModelVoiceTest.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/test/kotlin/com/handy/app/chat/ChatViewModelVoiceTest.kt:180): fixed a full-suite dispatcher cleanup race exposed by the audit.

Before/after in practical terms: before, Handy always asked Android STT for English with one result, so “panch minute ka timer lagao” could be poorly recognized and auto-sent. Now Hindi/Hinglish can route through `hi-IN`, Hinglish enables Android 14 code-mix extras where supported, STT asks for top 3 results, and low-confidence alternatives wait for user confirmation instead of going straight to the LLM.

Validation is clean:
- `git diff --check`
- targeted `AndroidSttClientTest`
- targeted `VoiceControllerTest`, `OverlayPresenterFsmTest`, `ChatViewModelVoiceTest`
- full `:core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`
- installed `app-debug.apk` on `emulator-5554`, launched Handy, and fresh logcat scan found no `FATAL EXCEPTION`, no `E/AndroidRuntime`, no ANR, no forced finish, no process death.

RCA and the detailed manual release plan are logged in [DEBUG_LOG.md](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/DEBUG_LOG.md:1959). The remaining checks are physical/manual speech checks: real network inspector for On-device only, actual Hindi/Hinglish utterances, and Android 14 Pixel code-mix recognition. The unrelated untracked `handy_macos_ref/` folder was left untouched.

### USER 2026-05-26T08:30:37.580Z

ensure on-device STT is really strong. do a deep audit of the code, the logic, the algo, and the complete wiring of the on-device speech-to-text and text-to-speech. I want you to use your highest level of reasoning and research. Feel free to search the web for best practices. Look at different forums, developer forums, voice AI forums, and come up with what are the different ways we can kind of make our on-device speech-to-text and text-to-speech really strong and accurate and very one of the best in the industry. Find such practices, list them out to me, and take the top highest confidence, highest impact levers and implement them. Share the before and after for each of the strengthening points that you pick up. And for each of the point and the code changes, for every code change, ensure that the pre and post code is very accurate, the wiring is thorough, and there are no breakage in the functional aspects of the current handyman. Ensure that there are no other issues that come up with the changes that you have done. Be very thorough, have highest attention to detail, and use the highest level of reasoning, thinking, and research skills possible. We want to have the best Android speech-to-text and text-to-speech available in the current app that we are building, the best possible with the best practices that are known to the industry. Deeply analyze all the code written by you in the past and the new code too in this session, and even after compacting of context, ensure that our code is really strong and does not break anything, and we are actually building a delta improvement in the performance. I would recommend even implementing logs and some basic evals so that we can measure the performance of our STT and TTS, the on-device STT and TTS. Do not download any extensive third party models or tools yet. do what is available within the android suite or simple light weight optimisations. we will use whisper.cpp and another local model in a future build. ensure the code is properly wired in and the transcription, thinking, tool call, navigation to coordniates, agentic recipes and response in text bubbles and chat are all srong and intact and working fine. audit all the recent code changes too and ensure the above is good.

### ASSISTANT 2026-05-26T08:30:48.733Z

I’ll do this as a research-backed hardening pass, not a vibes pass. I’m going to verify Android’s current official STT/TTS APIs first, then map those levers onto Handy’s existing STT/TTS wiring and only implement changes that are lightweight, high-confidence, and testable.

### ASSISTANT 2026-05-26T08:32:06.126Z

The official Android docs point to a few high-confidence levers: support/language probing on API 33+, contextual biasing strings and device-context biasing, Android 14 word confidence/language-switch telemetry, and TTS voice selection by offline/network requirement plus quality/latency. I’m mapping those onto the current Handy code now and I’ll avoid the tempting but risky knobs Android explicitly warns to use sparingly.

### ASSISTANT 2026-05-26T08:33:59.446Z

The code audit found two straightforward gaps worth fixing: the manifest queries speech recognizers but not TTS engines, and AndroidTtsClient uses the system default language without selecting the best installed embedded voice. I’m also adding STT biasing/telemetry extras that Android exposes specifically for recognition quality, while avoiding silence/endpointer knobs that the docs warn can create unexpected behavior.

### ASSISTANT 2026-05-26T08:37:19.019Z

The first code pass is in: STT now gives Android static command bias phrases, on-device device-context biasing, Android 14 language detection/word-confidence requests for Hinglish, and privacy-safe timing/quality logs. TTS now queries embedded voices and prefers installed offline voices by locale, quality, and latency, with script-based Hindi voice switching for Devanagari replies. I’m running focused tests now to shake out API or test-fixture mistakes.

### ASSISTANT 2026-05-26T08:39:28.131Z

I’m picking this back up from the audit pass where the STT/TTS hardening is implemented but the targeted Android runtime tests exposed a JVM/Robolectric gap around Android TTS framework types. I’ll fix that first, then run the broader validation so we’re not relying on hope in a voice path.

### ASSISTANT 2026-05-26T08:39:39.059Z

The failing tests are not pointing at the production TTS path; they’re plain JVM tests touching Android framework constants and `Voice`. I’m converting that test class to Robolectric so it exercises the same Android APIs without “method not mocked” failures.

### ASSISTANT 2026-05-26T08:40:14.100Z

The targeted STT/TTS runtime tests are now green. I’m broadening the audit to the wiring around settings, voice controller submission, overlay UI, dependency injection, and downstream chat/TTS flows before I run the full build.

### ASSISTANT 2026-05-26T08:42:48.391Z

I found one real tightening point in the STT route handoff: AUTO mode correctly removes `EXTRA_PREFER_OFFLINE` when falling back, but the newly added on-device context-bias extra could ride along into the system recognizer after fallback. I’m stripping that during cloud retries and adding a regression assertion so the privacy/route boundary stays crisp.

### ASSISTANT 2026-05-26T08:44:17.545Z

The Android API reference also turned up a subtle but important mismatch: `EXTRA_ENABLE_LANGUAGE_SWITCH` is documented as a string sensitivity value, not a boolean. I’m moving Hinglish to the documented balanced switch mode, which should make Android 14 code-mix handling more likely to actually engage.

### ASSISTANT 2026-05-26T08:45:12.603Z

Good, the route and Hinglish regressions are passing. I’m now running the affected app-level tests for low-confidence voice flow, overlay state, and voice-triggered TTS submission so the recognizer changes don’t accidentally break chat, tool dispatch, or panel behavior.

### ASSISTANT 2026-05-26T08:45:37.054Z

The app-level voice tests are green too. I’m doing the full safety sweep now: whitespace diff check, all core/runtime/app unit tests, lint, and debug APK assembly.

### ASSISTANT 2026-05-26T08:46:26.712Z

Full validation is still running, currently in lint analysis after all unit test tasks have completed. No failures have surfaced so far; I’m letting lint finish because that’s where Android API and manifest mistakes usually show up.

### ASSISTANT 2026-05-26T08:46:31.079Z

The full JVM/lint/build sweep passed. I’m doing a device-level smoke next: install the debug APK, launch the app, and scan logcat for startup crashes.

### ASSISTANT 2026-05-26T08:46:53.296Z

The APK installs and launches on the emulator; logcat showed no `AndroidRuntime` crash/ANR signatures and the Handy process is alive. I’m doing one last diff review and then I’ll write the RCA/audit entry into `DEBUG_LOG.md`.

### ASSISTANT 2026-05-26T08:49:27.147Z

Completed the deep speech audit and hardening pass. The strongest fixes landed in [AndroidSttClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidSttClient.kt:415), [AndroidTtsClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidTtsClient.kt:139), and the RCA/manual plan is logged in [DEBUG_LOG.md](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/DEBUG_LOG.md:1969).

**High-Impact Changes**
- STT biasing: before, Handy asked Android for generic recognition. After, API 33+ sessions include Handy command bias strings for timers, alarms, app-open, tap/search/send, scroll, and Hindi/Hinglish phrases like “panch minute ka timer lagao”.
- Hinglish: before, language switch used the prompt’s boolean sketch. After audit, it uses Android’s documented string mode `balanced`; docs say `EXTRA_ENABLE_LANGUAGE_SWITCH` is a string sensitivity value, not a boolean.
- Route privacy: before, the new device-context bias extra could have survived AUTO fallback because the same intent is reused. After, cloud/system retries strip `EXTRA_ENABLE_BIASING_DEVICE_CONTEXT`.
- STT eval logs: before, no measurable route/latency/confidence telemetry. After, logs capture mode, language, route, API, bias count, confidence, alternatives count, duration, ready latency, first partial latency, partial count, and RMS peak without transcript/audio.
- TTS voice quality: before, Android TTS relied mostly on default language behavior. After, it chooses an installed embedded/offline voice using `getVoices()`/`setVoice()` and sorts by locale match, quality, and latency. Android docs expose voice metadata, quality/latency, and network-required flags for this path.
- Hindi TTS: before, Hindi replies could stay on the default voice. After, Devanagari text switches to `hi-IN` and selects a Hindi system voice when installed.
- I intentionally did not add cloud STT, persist audio, download models, or tune silence/endpointer extras; Android warns those silence extras are rarely needed and can cause unexpected results.

**Audit Findings Fixed**
- Fixed documented API mismatch for Android 14 language switching.
- Fixed on-device context-bias leakage into AUTO cloud fallback.
- Added tests around those exact boundaries.
- Kept existing `SpeechRecognizer` lifecycle discipline: main-thread use and destroy-on-teardown are required by Android docs, and on-device recognizer creation still follows API 31+ behavior.

**Validation**
Passed:
- `git diff --check`
- `./gradlew --no-daemon :android-runtime:testDebugUnitTest --tests 'com.handy.runtime.speech.AndroidSttClientTest' --tests 'com.handy.runtime.speech.TtsChunkerTest' --stacktrace`
- `./gradlew --no-daemon :app:testDebugUnitTest --tests 'com.handy.app.voice.VoiceControllerTest' --tests 'com.handy.app.chat.ChatViewModelVoiceTest' --tests 'com.handy.app.overlay.OverlayPresenterFsmTest' --stacktrace`
- Full: `:core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`
- Installed `app-debug.apk` on `emulator-5554`, launched Handy, scanned logcat: no `FATAL EXCEPTION`, no `E/AndroidRuntime`, no ANR, process alive.

**Manual Release Plan**
Run the phrase suite across `AUTO`, `ON_DEVICE_ONLY`, and `NETWORK_ALLOWED`: “set a five minute timer”, “tap search”, “open WhatsApp”, “panch minute ka timer lagao”, “Subah saat baje alarm set kar do”. Verify route, fallback behavior, confidence, alternatives, and no transcript/audio in logs. Then test low-confidence chip gating, English/Hindi long TTS, barge-in while TTS is speaking, panel mic, widget long-press, full-chat mic, cancel, and mic-denied flows. Physical Android 14 Pixel testing is still the real accuracy gate for Hinglish code-mix.
