# GOAL

## Metadata

- Thread ID: `019e62f9-4bf3-7672-93a4-6940c601698f`
- Created: 2026-05-26 11:59:25 IST
- Updated: 2026-05-26 13:18:19 IST
- CWD: `/Users/satvik.bansal/Desktop/Handy.android/HandyV2`
- Source rollout: `/Users/satvik.bansal/.codex/sessions/2026/05/26/rollout-2026-05-26T11-59-25-019e62f9-4bf3-7672-93a4-6940c601698f.jsonl`
- Recorded git branch at start: `main`
- Recorded git SHA at start: `4c4026da01aab9ad4dc9f64077e6a795beb82eef`
- Messages exported: 60

## Brief Summary

This conversation focused on: Implement the Sarvam TTS provider that settings already expose. After P-VOICE-1 lands, Handy speaks via AndroidTtsClient. This prompt adds a second provider (Sarvam Bulbul v3) selectable in settings and a SwitchingTtsClient that routes by HandySettings.ttsPro… Likely related git changes: 4c4026d P-VOICE1: Wire voice responses to TTS.

## Git Commit Linkage

Commit links are heuristic: Codex records the starting git SHA, while likely related commits are inferred from timing plus title/commit-subject matching. Review the commit list before treating it as authoritative.

- Base SHA recorded by Codex: `4c4026da01aab9ad4dc9f64077e6a795beb82eef`
- Likely related commits:
  - `4c4026d` 2026-05-26 11:59:22 IST [high] P-VOICE1: Wire voice responses to TTS. Files: DEBUG_LOG.md, README.md, android-runtime/src/main/kotlin/com/handy/runtime/speech/AndroidTtsClient.kt, android-runtime/src/main/kotlin/com/handy/runtime/storage/DataStoreSettings.kt, android-runtime/src/test/kotlin/com/handy/runtime/speech/TtsChunkerTest.kt, app/src/main/kotlin/com/handy/app/agent/AgentSessionController.kt, app/src/main/kotlin/com/handy/app/chat/ChatViewModel.kt, app/src/main/kotlin/com/handy/app/overlay/FloatingWidgetOverlayService.kt, app/src/main/kotlin/com/handy/app/overlay/OverlayChatPanelService.kt, app/src/main/kotlin/com/handy/app/overlay/OverlayChatPipeline.kt, app/src/main/kotlin/com/handy/app/overlay/OverlayPresenter.kt, app/src/main/kotlin/com/handy/app/settings/SettingsActivity.kt

## Conversation

### USER 2026-05-26T06:29:42.880Z

GOAL
Implement the Sarvam TTS provider that settings already expose. After
P-VOICE-1 lands, Handy speaks via AndroidTtsClient. This prompt adds a
second provider (Sarvam Bulbul v3) selectable in settings and a
SwitchingTtsClient that routes by HandySettings.ttsProvider.

VERIFIED PRE-CONDITIONS
- core/src/main/kotlin/com/handy/core/model/Settings.kt
    enum class TtsProvider { SYSTEM("System (Default)"), SARVAM("Sarvam (Bulbul v3)") }
    enum class SarvamVoice { RITU(apiName="ritu"), RAHUL("rahul"), SIMRAN("simran") }
    HandySettings.ttsProvider default = SYSTEM
    HandySettings.sarvamVoice default = RITU
- android-runtime/.../EncryptedKeyStore (exists; used by Claude/Brave/Jina/GitHub keys)
- android-runtime/.../speech/AndroidTtsClient.kt:74-93 — sentence chunker
  (chunkOnSentenceBoundary). Reuse this; do NOT duplicate it.
- Sarvam Bulbul v3 REST endpoint: POST https://api.sarvam.ai/text-to-speech
    headers: api-subscription-key: <key>
    body: { "inputs": [<chunk>], "target_language_code": "en-IN" | "hi-IN" | …,
            "speaker": "ritu" | "rahul" | "simran", "pace": 1.0,
            "model": "bulbul:v2" (Sarvam currently exposes v2 publicly),
            "audio_format": "wav" }
    response: { "audios": ["<base64 wav>"], "request_id": "…" }
    max input length per chunk: 2500 chars. Use MAX_CHUNK = 2400 to stay safe.
  (Confirm the exact field names against current Sarvam docs at runtime — if
  the schema has shifted, the contract test in step 5 will catch it.)

IMPLEMENTATION

1. Add Sarvam key to EncryptedKeyStore namespace:
   File: android-runtime/.../EncryptedKeyStore.kt
       const val KEY_SARVAM = "sarvam_api_key"
   Surface set/get like the existing KEY_BRAVE etc.

2. Add HandySettings.sarvamSpokenLanguage:
   File: core/src/main/kotlin/com/handy/core/model/Settings.kt
       enum class SarvamLanguage(val code: String) {
           AUTO("auto"), ENGLISH("en-IN"), HINDI("hi-IN"), HINGLISH("hi-IN")
       }
       val sarvamSpokenLanguage: SarvamLanguage = SarvamLanguage.AUTO,
   AUTO = derive from device locale; HINGLISH = code-mix mode (still hi-IN
   target but tell the model the input is mixed; Sarvam handles this).

3. Audio playback abstraction:
   File: android-runtime/src/main/kotlin/com/handy/runtime/speech/AudioPlayback.kt
       interface AudioPlayback {
           suspend fun play(wavBytes: ByteArray, utteranceId: String)
           fun stop()
           fun release()
       }
   File: android-runtime/src/main/kotlin/com/handy/runtime/speech/MediaPlayerAudioPlayback.kt
       Use android.media.MediaPlayer. Write each chunk to a private cache
       file, hand the path to MediaPlayer, wait for onCompletion, then
       delete the file. Files live under context.cacheDir/tts/ and are
       deleted within 5s of playback. No persistent cache by default.

4. Implement SarvamTtsClient:
   File: android-runtime/src/main/kotlin/com/handy/runtime/speech/SarvamTtsClient.kt
       class SarvamTtsClient @Inject constructor(
           private val httpClient: OkHttpClient,
           private val keyStore: KeyStore,
           private val settings: DataStoreSettings,
           private val playback: AudioPlayback,
           private val json: Json,
           @ApplicationScope private val scope: CoroutineScope,
       ) : TtsClient {
           override val isSpeaking: Boolean get() = playbackActive.get()

           override fun speak(text: String, utteranceId: String) {
               val key = keyStore.get(EncryptedKeyStore.KEY_SARVAM)
                   ?: return delegateToSystemFallback(text, utteranceId)
               val s = settings.current()
               val chunks = AndroidTtsClient.chunkOnSentenceBoundary(text.trim(), maxChars = MAX_CHUNK)
               // serialize chunks; cancel any in-flight job
               currentJob = scope.launch { synthesizeAndPlaySerially(chunks, s, key, utteranceId) }
           }

           override fun stop() { currentJob?.cancel(); playback.stop() }
           override fun release() { stop(); playback.release() }

           private suspend fun synthesizeAndPlaySerially(
               chunks: List<String>, s: HandySettings, key: String, utteranceId: String,
           ) {
               chunks.forEachIndexed { i, chunk ->
                   val bytes = synthesizeOne(chunk, s, key) ?: run { fallbackError("synth_failed"); return }
                   playback.play(bytes, "$utteranceId-$i")
               }
           }
       }
   - HTTP timeouts: 8s connect, 15s read.
   - On 401: clear key from KeyStore via a separate callback, emit error
     "auth_failed", fall back to System TTS for this utterance (inject
     AndroidTtsClient or expose a fallback strategy via the switcher).
   - On 429: respect Retry-After if present, else immediate fall back.
   - On any failure: do NOT crash. Always have a fallback path.
   - NEVER log the API key.

5. SwitchingTtsClient:
   File: android-runtime/src/main/kotlin/com/handy/runtime/speech/SwitchingTtsClient.kt
       class SwitchingTtsClient @Inject constructor(
           private val system: AndroidTtsClient,
           private val sarvam: SarvamTtsClient,
           private val settings: DataStoreSettings,
           @ApplicationScope private val scope: CoroutineScope,
       ) : TtsClient {
           @Volatile private var active: TtsClient = system
           init {
               scope.launch {
                   settings.flow.collectLatest { s ->
                       val keyPresent = (s.ttsProvider == TtsProvider.SARVAM) &&
                                        keyStorePeek(EncryptedKeyStore.KEY_SARVAM) != null
                       active = if (keyPresent) sarvam else system
                   }
               }
           }
           override fun speak(text, id) {
               try { active.speak(text, id) }
               catch (t: Throwable) { Timber.w(t); system.speak(text, id) }
           }
           override fun stop() { active.stop(); system.stop() }   // both, defensive
           override fun release() { sarvam.release(); system.release() }
           override val isSpeaking get() = active.isSpeaking
       }

6. DI rewire:
   File: android-runtime/.../RuntimeModule.kt
   - Change `provideTtsClient` to return `SwitchingTtsClient` instead of
     `AndroidTtsClient`. AndroidTtsClient is now an internal dependency of
     SwitchingTtsClient.
   - Add `provideAudioPlayback` returning MediaPlayerAudioPlayback.

7. Settings UI:
   File: app/src/main/kotlin/com/handy/app/settings/sections/BrainSection.kt
   - Add TtsProvider picker (System / Sarvam).
   - Sarvam-only sub-rows (visible only when SARVAM picked):
     - Voice picker (Ritu / Rahul / Simran).
     - Language picker (Auto / English / Hindi / Hinglish).
     - Sarvam API key field (masked, reuses the existing key input pattern).
   - "Test voice" button — speaks a fixed sample
     ("Hello, I'm Handy. How can I help?" in English; switch text by
     language picker). Uses the active TtsClient through the switcher;
     this exercises the routing in production code, not a mock.

PRIVACY
- Generated audio is cached only in context.cacheDir/tts/ and deleted within
  5 seconds of playback completion or stop().
- Audit log records provider=sarvam, model, language, chars, latency_ms,
  request_id (Sarvam's), success/fail — NEVER the spoken text or the key.

ACCEPTANCE
- Selecting SYSTEM → AndroidTtsClient routes (verifiable via test hook).
- Selecting SARVAM with valid key → SarvamTtsClient routes.
- Selecting SARVAM with no key → settings UI shows "Add Sarvam key"
  hint; runtime falls back to System TTS without crashing.
- Voice picker change → next utterance uses new speaker.
- Network down during Sarvam call → System TTS speaks instead, no crash,
  audit notes the fallback.
- stop() while Sarvam is mid-call (network in flight) → request is
  cancelled and no audio plays.
- Long Hindi/Hinglish reply (>2500 chars) → chunked, played sequentially,
  no truncation.
- No raw audio files persist after stop()/release().
- Restarting the app retains the chosen provider.

TESTS
- SarvamTtsClientHttpTest (MockWebServer):
  - happy path: 200 with base64 wav → playback called with decoded bytes.
  - 401 → fallback emitted, key cleared callback fires.
  - 429 → no playback, fallback path.
  - 5xx → fallback.
  - >2500 char input → 2 HTTP calls in order, both played in order.
  - cancel mid-call → no playback.
- SwitchingTtsClientTest:
  - settings flow switch → active changes.
  - exception in active → falls back to system.
- ChunkerTest: verify SarvamTtsClient reuses AndroidTtsClient.chunkOnSentenceBoundary.
- Manual:
  - Add Sarvam key. Switch to Sarvam. Test voice button speaks.
  - Long Hindi-English mixed reply via voice — Sarvam speaks all of it,
    voice change applies on next utterance.

DO NOT
- Persist generated audio beyond playback.
- Log API keys.
- Ship a hardcoded fallback Sarvam key.
- Use ExoPlayer (heavy; we only need a 2-line MediaPlayer).
- Switch the default TtsProvider away from SYSTEM. Sarvam is opt-in.
- Speak typed-turn responses (P-VOICE-1 already gates this; do not
  weaken the gate here).

UPDATE
- DEBUG_LOG.md.
- README current-state: list Sarvam Bulbul TTS as available, gated by
  a Sarvam API key in settings. 

Additional stuff
A) Mention the change the code brings to the application - the before and after with real life practical examples if possible. 
B) Share an indepth end to end manual testing plan of the changes done - be practical and ensure all aspects are tested well before we proceed.   
C) ENSURE THERE ARE NO BUILDS ERRORS OR APP CRASHES. AUDIOT THE CODE COMPLETELY AND FIX ANY ERRORS

### ASSISTANT 2026-05-26T06:29:52.274Z

I’ll trace the current settings, TTS, key storage, and DI paths first, then wire the Sarvam provider into the existing patterns instead of inventing a parallel universe. I’ll also verify Sarvam’s current REST field names against their docs before locking in the request schema.

### ASSISTANT 2026-05-26T06:30:11.379Z

Sarvam’s current docs have drifted from the prompt’s older body shape: the REST endpoint still is `/text-to-speech`, but the field is now `text` (not `inputs`), `output_audio_codec` (not `audio_format`), and `bulbul:v3` is listed as available. I’m going to implement against that current contract and add MockWebServer coverage so any future schema wobble is loud.

### ASSISTANT 2026-05-26T06:31:06.812Z

The local shape is friendly: settings already carry `ttsProvider` and `sarvamVoice`; the missing pieces are persistence for the language, a secret slot, runtime clients, and UI rows. I’ve also spotted that the repo’s existing `KeyStore` abstraction is generic, so the new Sarvam “surface” can be a constant plus the same set/get/remove helpers already used for Claude/Brave/Jina/GitHub.

### ASSISTANT 2026-05-26T06:33:42.347Z

I’m going to start the scoped edits now: core gets the language enum and audit shape, runtime gets the Sarvam playback/client/switcher path, and then the app layer gets the settings controls. I’ll keep the generated-audio file handling very small and aggressive about cleanup.

### ASSISTANT 2026-05-26T06:41:14.125Z

The runtime and UI wiring are now in place, including the production switcher and the Sarvam HTTP client with cancellable OkHttp calls. I’m going to run the focused test/build compile pass next, because Kotlin and Hilt will quickly point out any constructor, enum, or MockWebServer API mismatches.

### ASSISTANT 2026-05-26T06:41:59.854Z

First compile found one Kotlin overload-resolution gotcha in the switcher test constructor: the injected constructor’s delegation was resolving back to itself. That’s a clean local fix by casting the concrete clients to the interface in the delegation call.

### ASSISTANT 2026-05-26T06:46:26.962Z

That pass surfaced two test-harness issues, not production wiring: MockWebServer 5 wants explicit `start()` before `url()`, and the switcher’s settings collector should live in `backgroundScope` in unit tests so `runTest` can finish cleanly. I’ve patched both and am rerunning the runtime tests.

### ASSISTANT 2026-05-26T06:48:19.575Z

Runtime tests are green now. Next I’m compiling the app layer, which is where Compose signatures, Hilt ViewModel injection, and the new settings controls will get checked.

### ASSISTANT 2026-05-26T06:49:32.690Z

The app unit tests compile and pass, and `git diff --check` is clean. I’m updating the README and debug log now so the repo’s current-state notes match the new opt-in Sarvam path and the exact test coverage we added.

### ASSISTANT 2026-05-26T06:52:01.969Z

The full validation is in the slow Android packaging stretch now; lint has already emitted its report and passed the `lintDebug` task. I’m letting assemble finish rather than calling it early, since that is the last line between “unit green” and “APK actually builds.”

### ASSISTANT 2026-05-26T06:54:31.208Z

The combined Gradle run appears to have wedged in the daemon after lint/tests, with the wrapper just waiting on the daemon socket and no new task output for several minutes. I’m going to stop that wrapper and rerun the remaining `:app:assembleDebug` as a separate no-daemon command so we get a clean build result instead of trusting a stuck process.

### ASSISTANT 2026-05-26T06:56:45.301Z

Implemented Sarvam Bulbul TTS end to end.

What changed:
- Before: Handy always spoke voice replies through Android system TTS, even though settings already had Sarvam enums.
- After: Handy defaults to System TTS, but Settings can opt into Sarvam Bulbul v3 with Sarvam key, voice, and language controls. Example: a Hinglish voice reply like “Kal ka reminder set kar do” can be spoken by Sarvam using the selected Ritu/Rahul/Simran voice, while missing key/network errors fall back to Android TTS without crashing.

Key implementation points:
- Added `SarvamLanguage`, persisted `sarvam_spoken_language`, and added `EncryptedKeyStore.KEY_SARVAM`.
- Added `AudioPlayback`, `MediaPlayerAudioPlayback`, `SarvamTtsClient`, and `SwitchingTtsClient`.
- Rewired DI so app-wide `TtsClient` is the switcher.
- Added Settings UI for provider, Sarvam voice, Sarvam language, Sarvam key, and Test voice.
- Added Sarvam audit rows with model/language/chars/latency/request_id/result, without spoken text or keys.
- Updated `README.md` and `DEBUG_LOG.md`.

I confirmed current Sarvam docs now use `text`, `output_audio_codec`, and `bulbul:v3`, so the implementation follows the current REST contract, not the older `inputs` / `audio_format` shape. Sources: [REST API](https://docs.sarvam.ai/api-reference-docs/text-to-speech/convert), [Bulbul model docs](https://docs.sarvam.ai/api-reference-docs/models/bulbul).

Validation passed:
- `git diff --check`
- `./gradlew :android-runtime:testDebugUnitTest --tests 'com.handy.runtime.speech.SarvamTtsClientHttpTest'`
- `./gradlew :android-runtime:testDebugUnitTest`
- `./gradlew :app:testDebugUnitTest`
- Full: `./gradlew :core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug --stacktrace`
- Follow-up compile/build after cleanup: `:android-runtime:testDebugUnitTest :app:assembleDebug` and `:android-runtime:compileDebugKotlin`

Manual test plan:
1. Fresh install/default: confirm System is selected, Sarvam rows hidden, voice replies use Android TTS, typed chat turns do not speak.
2. Select Sarvam with no key: confirm “Add key” hint, tap Test voice, verify fallback speech and no crash.
3. Add valid Sarvam key: Test voice should speak via Sarvam. Switch Ritu/Rahul/Simran and verify the next utterance changes voice.
4. Language matrix: test Auto, English, Hindi, Hinglish. For Auto, change device locale English/Hindi and confirm target behavior.
5. Long reply: trigger a >2500 char Hindi/Hinglish voice reply and confirm sequential playback without truncation.
6. Failure paths: turn network off during Sarvam, use an invalid key for auth clear/fallback, and confirm System TTS still speaks.
7. Stop behavior: start a long Sarvam utterance, then start listening/tap stop/dismiss panel; audio should stop and no later chunk should play.
8. Privacy cleanup: inspect `cacheDir/tts` after completion, stop, and release; WAV files should not persist.
9. Audit: verify Sarvam entries include provider/model/language/chars/latency/request_id/result and never include spoken text or the key.
10. Restart app: confirm provider, Sarvam voice, language, and masked key state persist.

Note: `handy_macos_ref/` was already untracked and I left it untouched.

### USER 2026-05-26T06:56:45.552Z

Lot of changes done above which included context compactions in the middle. 1400+ lines of code were written across 25+ files- that's a lot of code and it needs a deep deep thorough audit - functional as well as non-functional! The voice services especially are critical in Handy! Can you go through the original prompt, code changes done in extreme detail, audit them, ensure all the required changes were done and done correctly with no downstream impact. the code before this works fine with it - no suspicious lines of codes for app crashes, or faulty wirings. all the services that needed to be changed have been changed and nothing is left or missed. be very thorough in your analysis and evaluation. use highest level of reasoning available. Log the rca in the debug log and explain why the misses were missed. use highest level of thinking possible and think deeply. Evaluate and understand very deeply. Be very thorough and detailed. use your highest level of intelligence to analyse the requirement, code generated and plug gaps. be very thorough and have highest attention to detail. carefully analyse all the code - new and old and all dependencies too.

### ASSISTANT 2026-05-26T06:57:03.171Z

I’ll do this as a proper audit pass, not just a glance at the diff. I’m going to re-read the original requirements against the changed files, trace runtime wiring from Settings → DataStore/KeyStore → DI → switcher → Sarvam/System clients → playback/audit, and then patch any gaps I find before rerunning validation.

### ASSISTANT 2026-05-26T06:57:34.929Z

I rechecked the current official Sarvam docs during the audit: REST still uses `text`, `target_language_code`, `speaker`, `model`, and `output_audio_codec`, with `bulbul:v3` and 2500-char REST guidance. Now I’m reading the actual local implementation in context, including surrounding voice controller and speech-output paths, to catch integration issues that simple tests may not expose.

### ASSISTANT 2026-05-26T06:58:49.489Z

I’m going back through this as a post-implementation safety audit rather than a surface diff review. I already have two concrete edge cases worth tightening: direct `TtsClient.speak()` calls can overlap the old provider when switching, and old cache files should be swept defensively on stop/release/startup, not just tracked per playback.

### ASSISTANT 2026-05-26T06:59:03.939Z

The search map confirms production speech still funnels through `SpeechOutputController` for normal voice replies, with the settings test button as the main direct caller. I’m reading the provider, playback, DI, settings, and speech gate code together now so the audit catches wiring bugs, not just isolated unit behavior.

### ASSISTANT 2026-05-26T06:59:46.121Z

One requirement-detail shifted since the prompt: the current Sarvam docs now expose Bulbul `v3` REST with `text`, `target_language_code`, `speaker`, `model`, and `output_audio_codec`; the implementation is already using the current schema, which is good. I’m going to patch the two safety gaps and extend the tests so those cases stay locked down.

### ASSISTANT 2026-05-26T07:01:16.039Z

The first patch now makes the switcher explicitly stop whichever provider is not selected before it starts the chosen provider. That closes the settings “Test voice” overlap path without touching the normal voice-turn gate. The playback patch adds a stale WAV sweep on construction, stop, and release, which covers process-restart residue as well as active playback files.

### ASSISTANT 2026-05-26T07:02:10.738Z

While checking privacy beyond the new files, I noticed the global log redactor did not explicitly name Sarvam’s `api-subscription-key` header. The code does not log that header today, but this is exactly the sort of non-functional guardrail that should travel with a new API provider, so I’m adding it and covering it in the privacy test.

### ASSISTANT 2026-05-26T07:02:51.789Z

The two targeted Gradle invocations collided in the shared `android-runtime` KSP output and corrupted the generated cache. That is a build-output issue, not a source failure; I’m clearing only generated KSP/Gradle build artifacts for that module and rerunning serially so we get a clean signal.

### ASSISTANT 2026-05-26T07:03:08.899Z

KSP is still holding the old storage in a live Gradle daemon after the failed parallel run. I’m stopping the daemon and rerunning with `--no-daemon`; this keeps the cleanup limited to generated state and avoids muddying the source audit.

### ASSISTANT 2026-05-26T07:04:18.732Z

A deeper runtime race surfaced in `SarvamTtsClient`: a cancelled older synthesis job could clear the shared `playbackActive` flag after a newer utterance had already started. That would make the app think speech was idle during a live network/playback turn. I’m tying the active flag to a generation token now so stale jobs cannot stomp the current utterance state.

### ASSISTANT 2026-05-26T07:07:56.790Z

The audit now has runtime hardening in place: no stale provider overlap, no stale WAV cache, blank keys do not count as configured, stale Sarvam jobs cannot clear the current speaking state, and fallback audit rows now explicitly mark `fallback=system`. I’m moving into full-suite validation next; I’ll avoid parallel Gradle invocations from here because KSP does not tolerate two separate builds writing the same generated stores.

### ASSISTANT 2026-05-26T07:08:41.496Z

Full validation is still running; the build has compiled app/runtime and packaged `assembleDebug`, and it is in lint analysis now. No source errors so far.

### ASSISTANT 2026-05-26T07:10:08.479Z

One last edge in the audio cache path deserved tightening: if `stop()` or `release()` lands while a chunk file is being written, the stop sweep could run before the file appears. I’m adding a playback generation guard so a file written after stop/release is immediately deleted instead of becoming a leftover.

### ASSISTANT 2026-05-26T07:11:07.278Z

The write-race cache guard compiled cleanly and the Sarvam/switcher tests still pass. I’m rerunning the full command one more time after that last production-code tweak so the final state is fully validated.

### ASSISTANT 2026-05-26T07:12:12.457Z

Full unit/lint/build validation is green. I’m doing one last runtime sanity check now: if an emulator/device is attached, I’ll install and launch the debug APK and scan logcat for Handy crashes; if nothing is attached, I’ll state that clearly rather than pretending a manual launch happened.

### ASSISTANT 2026-05-26T07:13:58.541Z

**Audit Complete**

I did the deep pass and plugged additional gaps. The important fixes after the audit were:

- `SwitchingTtsClient` now stops the inactive provider before speaking, preventing System/Sarvam overlap from Settings Test Voice or direct TTS calls: [SwitchingTtsClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/SwitchingTtsClient.kt:55)
- `SarvamTtsClient` now cancels any in-flight job before every speak, handles stale coroutine state with a generation token, and keeps fallback/audit behavior safe: [SarvamTtsClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/SarvamTtsClient.kt:94)
- `MediaPlayerAudioPlayback` now sweeps stale WAV files on startup/stop/release and guards against files written during a racing stop/release: [MediaPlayerAudioPlayback.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/MediaPlayerAudioPlayback.kt:28)
- Sarvam fallback audits now explicitly include `fallback=system`, without spoken text or keys: [SarvamTtsClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/SarvamTtsClient.kt:366)
- Global log redaction now explicitly catches Sarvam’s `api-subscription-key`: [HandyApplication.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/HandyApplication.kt:193)
- RCA and “why missed” notes are logged in [DEBUG_LOG.md](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/DEBUG_LOG.md:1925)
- README current state now includes Sarvam availability and temporary-cache behavior: [README.md](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/README.md:109)

Current Sarvam docs confirm the implementation should use `POST /text-to-speech`, `text`, `target_language_code`, `speaker`, `model`, `output_audio_codec`, `bulbul:v3`, and the 2500-char limit, matching the final code. Sources: [Sarvam REST TTS docs](https://docs.sarvam.ai/api-reference-docs/text-to-speech/convert), [Sarvam Bulbul docs](https://docs.sarvam.ai/api-reference-docs/models/bulbul).

**Before / After**

Before: Handy always spoke through Android System TTS, even if settings exposed Sarvam. A user selecting “Sarvam” in settings had no real runtime route.

After: Handy defaults to System TTS, but if the user selects Sarvam and adds a key, voice replies route through Sarvam Bulbul v3. If the key is missing, invalid, rate-limited, or the network is down, Handy falls back to System TTS instead of crashing or going silent.

Practical example: a Hinglish voice reply longer than 2500 chars is chunked into safe Sarvam calls, played in order, and stopped cleanly if the user starts listening again.

**Validation Passed**

- `git diff --check`
- `./gradlew --no-daemon :android-runtime:testDebugUnitTest --tests 'com.handy.runtime.speech.SwitchingTtsClientTest' --tests 'com.handy.runtime.speech.SarvamTtsClientHttpTest' --stacktrace`
- `./gradlew --no-daemon :app:testDebugUnitTest --tests 'com.handy.app.privacy.CrashDiagnosticsFormatterTest' --stacktrace`
- Full: `./gradlew --no-daemon :core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug --stacktrace`
- Installed `app-debug.apk` on `emulator-5554`, launched `com.handy.android`, and logcat showed no `FATAL EXCEPTION`, no `E/AndroidRuntime`, no ANR, no forced Handy finish, and no Handy process death.

**Manual Test Plan**

1. Fresh install, leave provider as System, ask a voice question, confirm Android TTS speaks.
2. Switch provider to Sarvam with no key, confirm “Add key” hint appears and Test Voice falls back to System.
3. Add valid Sarvam key, select Sarvam, tap Test Voice, confirm Sarvam voice speaks.
4. Change Ritu/Rahul/Simran, tap Test Voice after each change, confirm next utterance changes speaker.
5. Test Auto/English/Hindi/Hinglish samples, especially Hinglish mixed text.
6. Speak a long Hindi/Hinglish response over 2500 chars, confirm sequential playback with no truncation.
7. Disable network during Sarvam call, confirm System fallback and audit fallback entry.
8. Use invalid key, confirm fallback and key clearing on auth failure.
9. During a long Sarvam call, tap stop/start listening/dismiss panel, confirm no delayed audio starts afterward.
10. After playback/stop/release, inspect app cache `cache/tts` and confirm no WAV files persist.
11. Restart app and confirm selected provider, voice, language, and key state persist.
12. Confirm typed chat turns still do not speak; only voice-triggered `ttsText` does.

### USER 2026-05-26T07:16:13.233Z

Prompt for Codex — Redesign the existing Voice section in Settings
You are redesigning the existing Voice section in Handy's Settings screen. Currently it lives inside the AI Brain card. Move it out into its own collapsible accordion section, sitting directly below AI Brain, matching the exact visual language of the other section cards (Capabilities, Automations, Privacy). Use a distinct honey-yellow accent to differentiate it from Brain's amber.

Files you will touch
HandyV2/app/src/main/kotlin/com/handy/app/settings/SettingsActivity.kt (or whichever file owns the Settings UI)
HandyV2/app/src/main/kotlin/com/handy/app/ui/theme/HandyDesign.kt (token additions)
Likely a new file: HandyV2/app/src/main/kotlin/com/handy/app/settings/VoiceSection.kt
Step 1 — Add honey tokens to HandyDesign.Colors
object HandyDesign {
    object Colors {
        // … existing tokens

        // Honey — secondary warm accent, used by the Voice section so it
        // reads as part of the warm family but distinct from Brain's amber.
        val Honey       = Color(0xFFF0C674)
        val HoneySoft   = Color(0x2EF0C674)   // ~18 % alpha
        val HoneyHair   = Color(0x5CF0C674)   // ~36 % alpha
        val HoneyInk    = Color(0xFF1F1709)   // text on solid honey
    }
}
Step 2 — Make HandySwitch tone-aware
The existing HandySwitch composable hardcodes Accent for its on-state. Add an optional tone: Color parameter that falls back to Accent:

@Composable
fun HandySwitch(
    checked: Boolean,
    onCheckedChange: (Boolean) -> Unit,
    modifier: Modifier = Modifier,
    enabled: Boolean = true,
    tone: Color = HandyDesign.Colors.Accent,        // NEW
    toneSoft: Color = HandyDesign.Colors.AccentSoft, // NEW
) {
    val onColor = if (enabled) tone else toneSoft
    // … rest of implementation unchanged, but swap accent for `onColor`
}
Update every call site that passes tone to also pass a matching toneSoft. The Voice section's Speak-replies switch passes tone = Honey, toneSoft = HoneySoft.

Step 3 — Make the pill-select component tone-aware
Find the existing component used for the "Triggers" pill row in the Automations section. It almost certainly hardcodes Accent/AccentSoft/AccentHair for selected pills. Add tone params:

@Composable
fun PillSelectRow(
    title: String,
    options: List<PillOption>,
    onSelect: (PillOption) -> Unit,
    modifier: Modifier = Modifier,
    tone: Color = HandyDesign.Colors.Accent,
    toneSoft: Color = HandyDesign.Colors.AccentSoft,
    toneHair: Color = HandyDesign.Colors.AccentHair,
)

data class PillOption(
    val label: String,
    val selected: Boolean,
    val tag: String? = null,         // e.g. "ADD KEY" or "SOON"
    val enabled: Boolean = true,
)
The pill rendering uses toneSoft for selected background, toneHair for selected border, tone for selected text. Unselected pills use SurfaceElevated + BorderSubtle + TextSecondary. The optional tag renders after the label in TextMuted at 9 sp uppercase letter-spaced 0.08em with " · " separator.

Step 4 — Build VoiceSection composable
Create VoiceSection.kt containing a single accordion card that mirrors CapabilitiesSection, AutomationsSection, etc. Section state:

sealed class VoiceProvider {
    object Off : VoiceProvider()
    object System : VoiceProvider()
    data class Sarvam(
        val apiKey: String?,                     // null = empty, non-null = saved
        val voice: SarvamVoice = SarvamVoice.Ritu,
        val language: SpokenLanguage = SpokenLanguage.Auto,
    ) : VoiceProvider()
}

enum class SarvamVoice { Ritu, Rahul, Simran }
enum class SpokenLanguage { Auto, English, Hindi, Hinglish }

data class VoiceSectionState(
    val provider: VoiceProvider,
    val expanded: Boolean,
)
Card structure
┌─────────────────────────────────────────────────┐
│ [♪]  Voice                                  ⌄  │  ← Header: tile + title + subtitle + chevron
│      Replies spoken · Sarvam · Ritu             │
├─────────────────────────────────────────────────┤
│ Speak voice replies aloud         [●━]          │  ← SwitchRow (honey)
├─────────────────────────────────────────────────┤
│ Voice provider                                  │  ← PillSelectRow (honey)
│ ( System ) ( Sarvam )                           │
├─────────────────────────────────────────────────┤
│ Sarvam voice                                    │  ← Only if provider is Sarvam
│ ( Ritu ) ( Rahul ) ( Simran )                   │
├─────────────────────────────────────────────────┤
│ Spoken language                                 │  ← Only if provider is Sarvam
│ ( Auto ) ( English ) ( Hindi ) ( Hinglish )     │
├─────────────────────────────────────────────────┤
│ SARVAM API KEY                                  │  ← Only if provider is Sarvam
│ ┌───────────────────────────────────────┐ 👁 📋 │
│ │ Paste your Sarvam key                 │      │
│ └───────────────────────────────────────┘      │
│ Add Sarvam key to use Bulbul. Handy falls back  │  ← Only if no key yet
│ to System until a key is saved.                 │
├─────────────────────────────────────────────────┤
│ Test voice                          ( Speak )   │  ← Only if Sarvam, disabled if no key
│ Match the device language                       │
├─────────────────────────────────────────────────┤
│ ● Add an API key to connect                  −  │  ← Status footer (honey if empty, green if ready)
└─────────────────────────────────────────────────┘
Subtitle copy logic
fun voiceSubtitle(provider: VoiceProvider): String = when (provider) {
    VoiceProvider.Off    -> "Replies are text-only"
    VoiceProvider.System -> "Replies spoken · System voice"
    is VoiceProvider.Sarvam -> when {
        provider.apiKey != null -> "Replies spoken · Sarvam · ${provider.voice}"
        else                     -> "Replies spoken · Sarvam · needs key"
    }
}
Header tile
Use the audio-lines Lucide icon. Convert it to a Vector Drawable resource at res/drawable/ic_audio_lines.xml. Tile is 44 dp, corner radius 12 dp, background HoneySoft, border Honey at 33 % alpha, icon at 22 dp tinted Honey.

Master toggle row
SwitchRow(
    title = "Speak voice replies aloud",
    checked = provider !is VoiceProvider.Off,
    onCheckedChange = { onSpeakRepliesToggle(it) },
    tone = HandyDesign.Colors.Honey,
    toneSoft = HandyDesign.Colors.HoneySoft,
)
When toggled off, this transitions provider to VoiceProvider.Off. When toggled on, restore the last selected provider (default System).

Voice provider pill row
PillSelectRow(
    title = "Voice provider",
    tone = HandyDesign.Colors.Honey,
    toneSoft = HandyDesign.Colors.HoneySoft,
    toneHair = HandyDesign.Colors.HoneyHair,
    options = listOf(
        PillOption("System", selected = provider is VoiceProvider.System),
        PillOption(
            label = "Sarvam",
            selected = provider is VoiceProvider.Sarvam,
            tag = if (provider is VoiceProvider.Sarvam && provider.apiKey == null) "Add key" else null,
        ),
    ),
    onSelect = { onSelectProvider(it) },
)
Sarvam-only sub-rows
Render only when provider is VoiceProvider.Sarvam. All three (voice picker, language picker, key field, test button) share the same honey tone.

The API key overline is plain uppercased mono-font text: "Sarvam API key" at 10 sp / 0.12em letter-spacing / TextMuted color. No key icon prefix — keep the overline clean and consistent with other API-key fields (Anthropic in Brain, Brave/Jina/GitHub in Capabilities).

The text field:

Placeholder: "Paste your Sarvam key"
When saved, display masked: first 3 chars + ·repeating dots + last 4 chars (e.g. "sk-····fddd")
Trailing: eye + copy icon buttons (existing pattern)
The helper caption (only when apiKey == null):

Add Sarvam key to use Bulbul. Handy falls back to System until a key is saved.

Style: 12 sp / 145 % line-height / TextSecondary color / 10 dp top margin.

The Test voice row:

Title: "Test voice"
Subtitle: "Match the device language"
Trailing: a 32 dp tall button labeled "Speak" — SurfaceElevated background, 12 dp radius, TextPrimary text at 13 sp semibold
Disabled state (when apiKey == null): opacity 0.55, text color TextMuted, cursor default. Tap is no-op.
Status footer
val connected = provider is VoiceProvider.System ||
                (provider is VoiceProvider.Sarvam && provider.apiKey != null)
val dotColor = if (connected) HandyDesign.Colors.Success else HandyDesign.Colors.Honey
val label = if (connected) "Connected & Ready" else "Add an API key to connect"
Render: 8 dp colored dot (with matching shadow glow), 10 dp gap, label at 13 sp semibold in dotColor, full-width, 12 / 16 / 14 dp padding (top / horizontal / bottom).

Step 5 — Wire into SettingsActivity
In the LazyColumn (or Column) that lists the sections, insert VoiceSection between BrainCard and CapabilitiesSection:

LazyColumn(verticalArrangement = Arrangement.spacedBy(14.dp)) {
    item { BrainCard(state.brain, onBrainAction) }
    item {
        VoiceSection(
            state = state.voice,
            onAction = onVoiceAction,
        )
    }
    item { CapabilitiesSection(state.capabilities, onCapAction) }
    item { AutomationsSection(state.automations, onAutoAction) }
    item { PrivacySection(state.privacy, onPrivacyAction) }
}
Step 6 — ViewModel changes
Update the existing SettingsViewModel (and SettingsUiState) to expose voice: VoiceSectionState. Existing actions inside Brain that controlled voice — remove them. The new actions:

sealed class VoiceAction {
    object ToggleSpeakReplies : VoiceAction()
    data class SelectProvider(val provider: VoiceProvider) : VoiceAction()
    data class SelectVoice(val voice: SarvamVoice) : VoiceAction()
    data class SelectLanguage(val language: SpokenLanguage) : VoiceAction()
    data class SetSarvamKey(val key: String) : VoiceAction()
    object ClearSarvamKey : VoiceAction()
    object TestVoice : VoiceAction()
    object ToggleExpanded : VoiceAction()
}
Persist provider, voice, language, and sarvamApiKey in your existing settings DataStore. Speak-replies on/off maps directly to provider == VoiceProvider.Off.

Step 7 — Error / empty / edge states
State	Behavior
Speak replies OFF	Card collapsed shows subtitle "Replies are text-only". Expanded shows only the toggle row (off). Status footer is hidden.
System provider, master ON	Card shows toggle (on) + pill row with System selected. No Sarvam-only sub-rows. Footer reads green "Connected & Ready".
Sarvam selected, key empty	Card shows full body with voice/language pickers, empty key field, helper caption, disabled Test button. Footer reads honey-yellow "Add an API key to connect". The Sarvam pill shows the "· ADD KEY" tag in muted color.
Sarvam selected, invalid key	Same as empty BUT helper caption becomes red and reads: "That key didn't work. Double-check it and try again." Test button stays disabled. Footer reads red Danger color, label: "Invalid key — falling back to System".
Sarvam selected, network error during validation	Helper caption: "Couldn't reach Sarvam. Check your internet and retry." Test button enabled but if tapped, retries validation. Footer: muted gray, "Offline — falling back to System."
Sarvam selected, key saved successfully	Key field shows masked value. No helper caption. Test button enabled. Footer green "Connected & Ready". The Sarvam pill drops its "· ADD KEY" tag.
TTS engine missing on device	(System provider only) Footer shows danger red dot + "System TTS unavailable on this device. Install a TTS engine in System Settings." with an underlined "System Settings" link that fires an intent to Settings.ACTION_VOICE_INPUT_SETTINGS.
Device offline + Sarvam selected	Footer overrides to muted "Offline — using cached voice cache" if cached audio exists, otherwise "Offline — falling back to System". Test button enabled but plays from local fallback.
Test voice tap while audio is playing	Speak button label switches to "Stop" + transitions to a small square stop icon. Tapping again cancels playback. Disabled state during the brief loading window (TTS engine waking up).
API key longer than ~80 chars	Truncate the masked display with ellipsis in the middle: sk-····…····fddd. Don't ever show the raw key.
Tap "Sarvam" pill while in System provider, no saved key	Switching to Sarvam reveals all sub-rows but leaves Voice/Language at last-used values (or defaults Ritu/Auto). Status footer immediately drops to honey "Add an API key to connect". Do NOT auto-focus the key field — let the user navigate intentionally.
Tap "System" pill while in Sarvam with saved key	Keep the saved Sarvam key in DataStore (don't clear). When user switches back to Sarvam later, restore the masked state. Voice subtitle updates to "Replies spoken · System voice".
Step 8 — Accessibility
The card root: Modifier.semantics { collapse/expand state via toggleableState } matching the existing accordion's pattern.
Master toggle: contentDescription = "Speak voice replies aloud" + state via Modifier.toggleable(...)
Each pill: Modifier.selectable(selected = ..., role = Role.RadioButton) so TalkBack announces "Voice provider · System · selected" or "· Sarvam · Add key · not selected"
API key field: contentDescription = "Sarvam API key", password masking respects TalkBack's "speak passwords" setting (use KeyboardOptions(autoCorrect = false, keyboardType = KeyboardType.Password))
Test button: when disabled, contentDescription = "Test voice. Disabled. Add a Sarvam API key first."
Status footer: liveRegion = LiveRegionMode.Polite so TalkBack announces transitions ("Connected & Ready" / "Invalid key")
Step 9 — Animations
Section expand/collapse: match the existing AnimatedVisibility + expandVertically(animationSpec = tween(220, easing = FastOutSlowInEasing)) already used by the other section cards. Don't introduce a new motion curve.
Pill selection: 160 ms Crossfade on the selected pill's background color when the user picks a new provider/voice/language.
Sub-row reveal (when switching System → Sarvam): AnimatedVisibility with expandVertically + fadeIn, 220 ms.
Status footer color transition: animateColorAsState(targetValue = dotColor, animationSpec = tween(240, easing = FastOutSlowInEasing)). Same for the label color.
Final QA checklist
 Voice section sits below AI Brain, has its own card with audio-lines tile in honey
 Brain stays amber; Voice is visibly distinct in honey-yellow
 No "Speak voice replies aloud" toggle remains inside the Brain card
 No "Voice provider" controls remain inside the Brain card
 All pill rows in Voice use honey for selected state (background HoneySoft, border HoneyHair, text Honey)
 Master toggle uses honey when on, surface-elevated when off
 Sarvam API key overline is plain text ("SARVAM API KEY") — no key icon prefix
 Helper caption appears only when Sarvam is selected and key is empty
 Test button is disabled (not absent) when no key is saved
 Status footer turns green only when System is active OR Sarvam+saved-key
 All seven error / edge states from Step 7 render correctly
 All TalkBack announcements work as specified
 Existing settings DataStore migration preserves any previously saved Sarvam key and voice/language selections
 No regression: Brain card height, Capabilities card layout, Automations card layout all identical to before
End of prompt. Run the existing settings screenshot tests after implementation to catch any unintended visual regressions in Brain/Capabilities/Automations cards.

### ASSISTANT 2026-05-26T07:16:22.934Z

I’ll treat this as a UI refactor on top of the Sarvam work, so first I’m locating the shared Settings section patterns and the existing switch/pill primitives. Then I’ll move the voice controls out of Brain with the least churn possible, keeping DataStore and runtime wiring intact.

### ASSISTANT 2026-05-26T07:16:49.214Z

The settings UI already has a shared `settings.design` primitive layer, so I’m going to extend that rather than make Voice a one-off. I also found the actual token file is `app/design/HandyDesignTokens.kt`, not the path named in the prompt.

### ASSISTANT 2026-05-26T07:18:37.127Z

I found the voice controls are currently embedded in `BrainSection` and the shared primitives are in `SettingsPrimitives.kt`. I’m going to make `SectionTone`, switch rows, and pill rows tone-aware first, then add a honey `VoiceSection` and remove the old Brain voice block.

### ASSISTANT 2026-05-26T07:24:12.471Z

The first compile is clean. The redesign now has its own honey section and Brain no longer owns the voice controls. I’m doing a second pass on state, accessibility, and the “edge state” rendering so the code is honest about what runtime state exists today and still has UI paths for the requested statuses.

### ASSISTANT 2026-05-26T07:26:05.102Z

I’ve got the implementation state from the compaction. I’m going to do the audit pass now: read the changed UI/ViewModel files against the prompt, run targeted searches for any leftover Brain voice controls, then run the heavier Gradle checks and device smoke if the build holds.

### ASSISTANT 2026-05-26T07:26:15.095Z

The first search confirms the old Brain file no longer contains the voice strings; those strings now live in the new Voice section and ViewModel. I also checked for screenshot/golden test infrastructure: there does not appear to be an existing Settings screenshot suite, only an unrelated diagnostics screenshot test, so I’ll validate with Gradle and a device smoke screenshot instead.

### ASSISTANT 2026-05-26T07:27:59.436Z

A few subtle issues popped out in the audit pass, not compile blockers but real polish/behavior mismatches: Sarvam’s masked key would double the hyphen for `sk-...` keys, the System TTS footer link was not underlined/full-copy, and the test-voice loading state could clear too early while Sarvam was still in network synthesis. I’m patching those before running the heavy checks.

### ASSISTANT 2026-05-26T07:29:18.054Z

The targeted compile is green after the fixes. I’m expanding validation now: diff hygiene, relevant unit tests across `core`, `android-runtime`, and `app`, lint, then a debug APK build and launch check if those pass.

### ASSISTANT 2026-05-26T07:29:38.917Z

Unit tests are green across `core`, `android-runtime`, and `app`. Next I’m running lint plus a full debug APK assemble; after that I’ll install/launch the Settings screen and capture the actual UI so we’re not only trusting compilation.

### ASSISTANT 2026-05-26T07:30:05.641Z

The assemble run caught a real resource issue that the earlier incremental compile missed: three existing screens reference `R.drawable.ic_brain`, but that drawable is absent in the current resource set. This is exactly the kind of downstream break the heavier pass is meant to expose, so I’m adding the missing vector instead of touching those call sites.

### ASSISTANT 2026-05-26T07:31:34.213Z

The missing `ic_brain` reference is fixed now, and the full `lintDebug + assembleDebug` pass is green. I’m moving to the device smoke check: install the debug APK, open Settings directly, and capture a screenshot of the new Voice placement.

### ASSISTANT 2026-05-26T07:31:55.449Z

The APK installed cleanly. Directly starting `SettingsActivity` from adb is blocked because the activity is intentionally non-exported, so I’m going through the exported launcher path and will navigate from there instead of loosening manifest security.

### ASSISTANT 2026-05-26T07:32:36.641Z

The device screenshot caught an actual layout bug: Compose’s `AnimatedVisibility` was overlaying the Sarvam-only sibling rows because they were emitted as multiple roots. That made voice/language/key/test rows pile on top of each other. I’m wrapping those rows in a single Column and re-running the build plus screenshot.

### ASSISTANT 2026-05-26T07:35:36.512Z

The fixed screenshot now shows the Voice card as intended: separate honey section below AI Brain, Sarvam voice/language/key/test rows stacked correctly, and Capabilities starting below it. I’m doing one small interaction check next: switch Voice provider to System and back to Sarvam to verify conditional rows hide/show without breaking the layout.

### ASSISTANT 2026-05-26T07:36:02.195Z

The interaction check behaved correctly: System selection hid Sarvam-only rows and kept the green footer; switching back restored Sarvam rows and the saved masked key. I’m doing one final unit-test rerun plus whitespace check now, since the layout patch landed after the earlier test pass.

### ASSISTANT 2026-05-26T07:36:41.845Z

Implemented and audited the Voice settings redesign.

The Voice controls now live in their own honey-accented accordion directly below AI Brain, with:
- honey design tokens and tone-aware switches/pills
- new `VoiceSection`
- Brain voice controls removed
- Sarvam voice/language/key/test rows moved into Voice
- settings ViewModel actions wired to persisted TTS provider, Sarvam voice/language, encrypted key, and active `TtsClient`
- `ic_audio_lines.xml` added
- accessibility semantics added for the master switch, pills, API key field, and status footer

During the deep pass I found and fixed three non-obvious issues:
- Sarvam rows were overlapping because `AnimatedVisibility` was receiving multiple root children. Wrapped them in a `Column`.
- saved Sarvam key masking could render an extra hyphen for `sk-...` keys. Fixed to first 3 chars + dots + last 4.
- full assemble exposed a stale/broken `ic_brain` vector state. Restored it to a valid vector so existing Brain references compile.

Validation passed:
- `git diff --check`
- `./gradlew --no-daemon :core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest --stacktrace`
- `./gradlew --no-daemon :app:lintDebug :app:assembleDebug --stacktrace`
- Installed debug APK on emulator successfully
- Opened Settings through the exported launcher path
- Verified screenshot visually: Voice appears below AI Brain, honey-accented, no overlap
- Toggled provider System → Sarvam: Sarvam-only rows hide/show correctly and saved key is retained

Settings screenshot captured at `/tmp/handy-settings-voice-fixed.png`.

### USER 2026-05-26T07:36:42.112Z

show 'connected and ready' only when the api key is entered. 
Also, 

Lots of changes done above which included context compactions in the middle. 1000+ lines of code were written across 10+ files- that's a lot of code and it needs a deep deep thorough audit - functional as well as non-functional! The voice services especially are critical in Handy! Can you go through the original prompt, code changes done in extreme detail, audit them, ensure all the required changes were done and done correctly with no downstream impact. the code before this works fine with it - no suspicious lines of codes for app crashes, or faulty wirings. all the services that needed to be changed have been changed and nothing is left or missed. ensure the ui is also proper with correct spacings, colours, fonts and elements. be very thorough in your analysis and evaluation. use highest level of reasoning available. Log the rca in the debug log and explain why the misses were missed. use highest level of thinking possible and think deeply. Evaluate and understand very deeply. Be very thorough and detailed. use your highest level of intelligence to analyse the requirement, code generated and plug gaps. be very thorough and have highest attention to detail. carefully analyse all the code - new and old and all dependencies too.

### ASSISTANT 2026-05-26T07:36:58.649Z

I’ll make the footer wording change first, then do the audit pass against the original Voice redesign and the earlier Sarvam TTS wiring. I’ll also add a DEBUG_LOG RCA entry for the misses found during this round, especially anything that could have slipped through incremental compile or visual assumptions.

### ASSISTANT 2026-05-26T07:37:21.821Z

I found the exact source for the footer behavior: `SettingsViewModel.toVoiceSectionState()` maps the System provider to `Connected`, so the Voice footer shows green even with no Sarvam key. I’m changing that state model so “Connected & Ready” is reserved for Sarvam with a saved key, and System gets neutral local-status copy instead.

### ASSISTANT 2026-05-26T07:40:08.874Z

I also tightened one runtime edge while auditing: `SwitchingTtsClient.isSpeaking` now reports either provider, not only the current active provider. That avoids a stop/test UI blind spot if settings switch while the previous provider is still playing. I added a regression test for that case.

### ASSISTANT 2026-05-26T07:41:25.656Z

The line-by-line UI audit shows the major prompt items are now covered, but I’m tightening one accessibility detail while I’m here: the pill rows used `Role.RadioButton`, but they did not include the row title in the announced description. I’m making the pill semantics carry “Voice provider · Sarvam · ADD KEY” style context without changing the visual layout.

### ASSISTANT 2026-05-26T07:42:08.894Z

The code audit also confirms the current Sarvam request shape matches Sarvam’s live docs, which now use `text`, `target_language_code`, `speaker`, `model`, and `output_audio_codec` for `/text-to-speech`; that differs from the older `inputs/audio_format` shape in the earlier prompt, so the implementation is intentionally on the current schema. I’m now running the full validation suite again after the footer and accessibility fixes.

### ASSISTANT 2026-05-26T07:43:08.086Z

Full tests, lint, and debug assemble are green after the latest patches. I’m doing the emulator smoke now to verify the changed footer copy: Sarvam with a saved key may show “Connected & Ready”; System should no longer show that line.

### ASSISTANT 2026-05-26T07:44:57.659Z

I’m picking up from the audit pass and finishing the last device-level check: specifically the Voice footer state after switching from Sarvam back to System. Then I’ll log the RCA in `DEBUG_LOG.md` and do a final sanity run over the touched files.

### ASSISTANT 2026-05-26T07:45:19.547Z

The device is attached, and the working tree is the expected large dirty tree from the Sarvam + settings work. I’m checking the tail of the debug log now so the new RCA entry lands in the right sequence instead of editing older history.

### ASSISTANT 2026-05-26T07:45:34.508Z

The current settings screen is expanded on Voice with Sarvam selected and a saved key, so `Connected & Ready` is correct there. I’m switching the provider to System on-device now to verify the new rule: System should no longer use that “connected” footer.

### ASSISTANT 2026-05-26T07:46:23.664Z

The settings screenshot-test request doesn’t have a matching settings screenshot suite in this tree; only a diagnostics screenshot smoke test exists. I’ve covered the visual regression with emulator install + UIAutomator/screenshot smoke instead, and I’m writing that limitation into the debug log rather than pretending the suite exists.

### ASSISTANT 2026-05-26T07:47:11.998Z

RCA is now appended as `DL-VOICE-5`, including the status-copy miss, switcher state miss, accessibility semantics miss, and the no-settings-screenshot-test limitation. I’m doing the final whitespace/status sanity pass now.

### ASSISTANT 2026-05-26T07:48:19.489Z

Done. I tightened the Voice status rule and completed the audit pass.

The key behavior change: Voice no longer says `Connected & Ready` for System TTS. System now shows `Using System voice`; `Connected & Ready` is only used when Sarvam is selected and a nonblank Sarvam key is saved. That mapping is in [SettingsViewModel.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/settings/SettingsViewModel.kt:388) and rendered in [VoiceSection.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/app/src/main/kotlin/com/handy/app/settings/sections/VoiceSection.kt:592).

I also fixed three audit findings:
- `SwitchingTtsClient.isSpeaking` now reports either provider speaking, not just the currently selected provider, so provider switches cannot hide active audio state. See [SwitchingTtsClient.kt](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/android-runtime/src/main/kotlin/com/handy/runtime/speech/SwitchingTtsClient.kt:52).
- Voice pill accessibility now includes row context, e.g. `Voice provider · Sarvam`, not only `Sarvam`.
- Sarvam key masking no longer exposes too much of very short entered values.

I logged the RCA and prevention rules in [DEBUG_LOG.md](/Users/satvik.bansal/Desktop/Handy.android/HandyV2/DEBUG_LOG.md:1940), including why the misses happened: conflicting status semantics from the UI prompt, routing tests that did not check post-switch speaking state, screenshot smoke not covering TalkBack semantics, and normal long-key fixtures hiding the short-key masking edge case.

Validation passed:
- `git diff --check`
- targeted compile/test for settings + switcher
- full `:core:test :android-runtime:testDebugUnitTest :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`
- installed on emulator, opened Settings, verified Sarvam+saved-key shows `Connected & Ready`, System shows `Using System voice`, and Sarvam-only rows disappear for System
- fresh logcat scan found no `FATAL EXCEPTION`, `AndroidRuntime`, ANR, or Handy process-death entries

I also rechecked Sarvam’s current TTS schema against the official [Sarvam text-to-speech docs](https://docs.sarvam.ai/api-reference-docs/text-to-speech/convert); the implementation is using the current `text` / `target_language_code` / `speaker` / `model` / `output_audio_codec` contract.
