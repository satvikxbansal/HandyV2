<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Phase 0 skeleton. Real permissions, foreground-service types, the
      <queries> block for android.speech.RecognitionService, the
      AccessibilityService declaration, the floating-widget overlay service,
      the MediaProjection capture service, Activities, and the
      <property name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" />
      element are all added in Phase 3.
    -->

    <application
        android:name=".HandyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoDisplay" />
</manifest>
