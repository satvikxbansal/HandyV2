<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Permissions ============================================= -->

    <!-- Voice -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Overlay widget -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Notifications for foreground services (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets Clock apps accept Handy's ACTION_SET_ALARM / ACTION_SET_TIMER handoff. -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- Foreground services (OS-1) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Internet access for Claude SSE -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Contacts recipes resolve local contact URIs and phone numbers, then hand off to Contacts/Dialer/SMS drafts. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- ===== Package-visibility queries (targetSdk 35) =============== -->
    <!-- Keep minimal; do NOT request QUERY_ALL_PACKAGES. -->
    <queries>
        <!-- Speech recognition providers (guardrails STT rule) -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <!-- Text-to-speech engines (Android 11+ package visibility) -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- Timer / alarm / maps / browser / dialer / email / share fan-out -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="google.navigation" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data
                android:host="com.android.contacts"
                android:mimeType="vnd.android.cursor.item/contact"
                android:scheme="content" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data
                android:mimeType="image/*"
                android:scheme="content" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data
                android:host="com.android.calendar"
                android:mimeType="vnd.android.cursor.item/event"
                android:scheme="content" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_CALCULATOR" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_GALLERY" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.WEB_SEARCH" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_TIMER" />
        </intent>
        <!-- Android Settings recipe deep links. Policy still blocks sensitive targets before dispatch. -->
        <intent>
            <action android:name="android.settings.APPLICATION_DETAILS_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.ACCESSIBILITY_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.ACTION_NOTIFICATION_LISTENER_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.IGNORE_BATTERY_OPTIMIZATION_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.DISPLAY_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.WIFI_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.BLUETOOTH_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.SECURITY_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.BIOMETRIC_ENROLL" />
        </intent>
        <intent>
            <action android:name="android.settings.APPLICATION_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.SOUND_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.ZEN_MODE_SETTINGS" />
        </intent>
        <!-- Food-delivery recipe deep links. Narrow package visibility only; no QUERY_ALL_PACKAGES. -->
        <package android:name="in.swiggy.android" />
        <package android:name="com.application.zomato" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="swiggy" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="zomato" />
        </intent>
    </queries>

    <application
        android:name=".HandyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Handy"
        android:usesCleartextTraffic="false"
        tools:targetApi="36">

        <!-- ===== Activities ===== -->

        <activity
            android:name=".onboarding.OnboardingActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Handy.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".onboarding.ActionDisclosureActivity"
            android:exported="false"
            android:theme="@style/Theme.Handy" />

        <activity
            android:name=".chat.ChatActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Handy"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Handy" />

        <activity
            android:name=".settings.SideBubblePreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.Handy" />

        <activity
            android:name=".diagnostics.DiagnosticsActivity"
            android:exported="false"
            android:theme="@style/Theme.Handy" />

        <activity
            android:name=".diagnostics.AuditReviewActivity"
            android:exported="false"
            android:theme="@style/Theme.Handy" />

        <!-- ===== Foreground Services (OS-1) ===== -->

        <!-- AssistantForegroundService: lifecycle anchor + microphone owner -->
        <service
            android:name=".service.AssistantForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse|microphone">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="@string/special_use_fgs_subtype" />
        </service>

        <!-- MediaProjectionCaptureService: screen capture fallback (API 26-29) -->
        <service
            android:name=".service.MediaProjectionCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- ===== AccessibilityService ===== -->

        <service
            android:name=".accessibility.HandyAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ===== Overlay widget service ===== -->

        <service
            android:name=".overlay.FloatingWidgetOverlayService"
            android:exported="false" />

        <!-- ===== V2 overlay chat panel service (scope §2) ===== -->

        <service
            android:name=".overlay.OverlayChatPanelService"
            android:exported="false" />

        <!-- ===== V2 Quick Settings tile (scope §11.1) ===== -->

        <service
            android:name=".tile.HandyQuickSettingsTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_compass"
            android:label="Handy"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- ===== V2 optional Assist entry (scope §11.3) ===== -->

        <service
            android:name=".assist.HandyAssistIntentService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- ===== V2 notification listener (scope §8) ===== -->

        <service
            android:name=".notifications.HandyNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
